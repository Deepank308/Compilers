"asc"

A B C = D E F G H I J + K L M
float short
restrict
do int switch
DEEPaNK123
const goto sizeof Bool
\n
	break;
"Compiler's Lab"
n o p q . r s t u v w x y z
'abc'
/ % << >> < > <= >= == != ^ | && ||
// Case for character constant
else register union
->
? : ; ...
double long typedef
23.89
.2
//////////
case volatile
// Hi there
char for signed while
N O P Q R ( S T ) U V W X Y Z
// Hi just another comment

break extern return void
\a \b \f \n \r \t \v
	printf("<CHARACTER CONSTANT, %d, %s>\n",token, yytext); 
Imaginary
= *= /= %= += -= <<= >>= &= ^= |=
2222
56

case CHAR_CONSTANT: 


default inline struct _Imaginary
'a'
a b c d e f g h i ! j k l m
;;;;;
// _Imaginary is keyword but Imaginary is identifier

auto enum unsigned
2
/*//////// Testing comment ///////*/
main int
continue if static Complex
++ -- & * + - ~ !
2.

[ ] ( ) { } . ->

void comment(void) {
	char c, prev = 0;
	while ((c = input()) != 0) {     /* (EOF maps to 0) */
		if (c == '/' && prev == '*')
			return;
		prev = c;
	}
	error("[ERROR]:INVALID COMMENTING SYNTAX");
}
